{
  "name": "edubot",
  "version": "1.0.0",
  "description": "EduBot USCS - Chatbot educacional com Typebot e Netlify Functions",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:functions": "netlify functions:serve",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:functions\"",
    "build": "cd frontend && npm install && npm run build",
    "install:all": "npm install && cd frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "netlify-cli": "^17.0.0"
  }
}
